--Campo de texto:
--text(tamanho variavel que armazena uma grande quantidade de caracteres)
--varchar(tamanho variavel que armazena de 0 a 255 caracteres)
--char(tamanho fixo que armazena de 0 a 255 caracteres)

--Campos numericos:
--int(valores numericos inteiros)
--float(valores numericos fracionados)

--Campos de data e hora:
--date(yyyy/mm/dd)
--time(hora)
--datatime(date e time no mesmo tempos)